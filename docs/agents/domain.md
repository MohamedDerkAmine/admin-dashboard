# Domain Docs

This repo uses a single-context documentation layout.

When present, agents should read:

- `CONTEXT.md` for domain language and product context.
- `docs/adr/` for architectural decisions.

If those files do not exist, infer domain language from the codebase and update documentation only when explicitly requested.
